You are optimizing the next anchor code for a real 128-GPU historical sparse-MoE workload.

Current anchor:
- historical sparse-MoE baseline
- top-2 routing
- fixed-capacity token dropping
- padded cross-rank dispatch buffers
- primitive nn.MultiheadAttention causal attention

Round-0 measured evidence from the real cluster:
- benchmark protocol: 2 train iterations, 1 warmup step, 1 measured step
- measured step_time_s: 42.3347
- tokens_dropped: 43704
- tokens_rerouted: 3845
- load_balance_cv: 3.4718
- remote_dispatch_ratio: 0.9923
- estimated_a2a_bytes: 504053760

Optimization goal for this round:
- reduce step_time_s by at least 15 percent if possible
- keep top-2 routing semantics intact in this round
- do not change train.py or config.py unless absolutely necessary
- prefer dispatch/runtime improvements before routing-policy changes

Strong suspicion:
- the padded all-to-all dispatch path is wasting bandwidth and memory movement
- almost all routed traffic is remote, so reducing padded communication overhead is high leverage

Suggested optimization direction:
- replace padded expert dispatch with packed variable-length dispatch
- preserve the external benchmark interface and the reported metrics
- keep the workload reviewable and close to the baseline style

Editable files this round:
- tests/workloads/historical_sparse_moe/model.py

Expected output:
- directly modified anchor code, not candidate IDs
- keep the code runnable on the existing 128-GPU launcher
- keep comments minimal and useful
