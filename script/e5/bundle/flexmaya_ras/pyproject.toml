[build-system]
requires = ["setuptools>=68", "wheel", "pybind11"]
build-backend = "setuptools.build_meta"

[project]
name = "flexmaya-ras"
version = "0.1.0"
description = "Maya-based FlexEva RAS prototype with C++ hook-memory trace core"
requires-python = ">=3.10"
